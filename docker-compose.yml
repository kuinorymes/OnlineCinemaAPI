services:
  redis:
    image: redis:latest
    container_name: "redis_container"
    ports:
      - "6379:6379"
    networks:
      - "cinema_api_network"

networks:
  cinema_api_network:
    driver: bridge
